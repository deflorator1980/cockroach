spring:
  datasource:
    url: jdbc:postgresql://localhost/jpa
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    show-sql: true


logging:
  level:
    org.hibernate.engine.jdbc.env.internal.LobCreatorBuilderImpl: ERROR